# Load these modules on the login node before submitting:
# Required
apptainer              # container runtime used by slurm/submit.sbatch
cuda                   # matches the GPU node driver; needed for nvidia-container-bindings

# Optional (enable only if you plan to profile inside the container)
nsight-systems         # for PROFILER=nsys
perf                   # for PROFILER=perf
